# Anvil-live-Prototype
Single-file, single-user demo of Anvil's core loop: upload a CSV, pick a target column, AutoML trains 5 algorithms and picks a winner, test predictions live. No auth, no Postgres, no Docker required — this is the 5-minute-demo version, not the production app.
